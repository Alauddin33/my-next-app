import { TextField } from "@mui/material";

const addtask = () => {
    return (
        <div>
            <TextField label="fullWidth" id="fullWidth" />
            <TextField label="fullWidth" id="fullWidth" />
            <TextField label="fullWidth" id="fullWidth" />
            <TextField label="fullWidth" id="fullWidth" />
        </div>
    );
};

export default addtask;